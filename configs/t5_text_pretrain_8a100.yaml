defaults:
  - base_text_pretrain

data:
  batch_size: 8
  batch_size_val_test: 4

max_epochs: 80
devices: [2, 3, 4, 5, 6, 7]
precision: bf16-mixed

datamodule:
  num_workers: 8

engine:
  optimizer:
    lr: 1e-4
