defaults:
  - modules: default
  - engine: default
  - data: ph14t_keywords
  - _self_

hydra:
  run:
    dir: outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}

max_epochs: 100
debug: True
devices: [0, 1]

itc_flag: True
itc_weight: 1.0

mlm_flag: True
mlm_mask_ratio: 0.40
mlm_weight: 1.0

prompt_learning: False
