defaults:
  - base_siam_train

data:
  batch_size: 1
  batch_size_val_test: 4

shifts: 2

max_epochs: 80
devices: [2, 3, 4, 5, 6, 7]
precision: bf16-mixed

datamodule:
  num_workers: 8

engine:
  optimizer:
    lr: 1e-6
