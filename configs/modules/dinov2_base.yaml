visual_encoder:
  _target_: modules.DinoV2Backbone
  id: facebook/dinov2-with-registers-base
  output_layer: -2

visual_hidden_size: 768

visual_adapter:
  _target_: modules.TokenSampleAdapter
  hidden_size: ${modules.visual_hidden_size}
  num_heads: 8
  num_layers: 4
  num_extra_queries: 4

predictor_depth: 2
